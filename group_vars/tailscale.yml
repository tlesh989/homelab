---
tailscale_authkey: "{{ lookup('env', 'TAILSCALE_KEY') }}"
tailscale_args: "--advertise-exit-node --advertise-routes=192.168.233.0/24 --accept-routes"

# LXC has no CAP_SYS_TIME; ntpsec cannot start and the host clock is shared.
ntp_enabled: false
