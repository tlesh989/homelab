---
# Doppler secrets required: CF_DNS_API_TOKEN_NET (Zone:DNS:Edit, scoped to tlesh.net only), DDNS_DOMAINS, PUSHOVER_API_TOKEN, PUSHOVER_USER_KEY
ddns_image_version: "1"
ddns_domains: "{{ lookup('env', 'DDNS_DOMAINS') }}"
ddns_proxied: "false"
