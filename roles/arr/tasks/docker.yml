---
# Docker Engine itself is installed by the geerlingguy.docker role at the
# play level (main.yml) — only the Python SDK for community.docker modules
# is this role's concern, matching every other Docker-host role.
- name: Install Docker Python SDK
  ansible.builtin.package:
    name: python3-docker
    state: present
