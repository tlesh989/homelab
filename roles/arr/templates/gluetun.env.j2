WIREGUARD_PRIVATE_KEY={{ lookup('env', 'PROTONVPN_PRIVATE_KEY') | mandatory('PROTONVPN_PRIVATE_KEY must be set via Doppler') }}
SERVER_COUNTRIES={{ lookup('env', 'PROTONVPN_SERVER_COUNTRIES') | mandatory('PROTONVPN_SERVER_COUNTRIES must be set via Doppler') }}
