---
# handlers file for ax88179b

- name: Reload udev rules
  ansible.builtin.shell:
    cmd: udevadm control --reload-rules && udevadm trigger
    executable: /bin/bash
  changed_when: true

- name: Load ax_usb_nic module
  community.general.modprobe:
    name: "{{ ax88179b_module_name }}"
    state: present
