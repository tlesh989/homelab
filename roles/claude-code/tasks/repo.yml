---
- name: Clone homelab repository
  ansible.builtin.git:
    repo: https://github.com/tlesh989/homelab.git
    dest: "{{ claude_code_repo_dest }}"
    version: main
    update: false
  become: true
  become_user: tommy
